// Test fixture for formatting - unformatted input.
// Contains: trailing whitespace, space indentation, extra blank lines,
// alignment candidates, relationships, inline comments.
schema "formatting"

import "./other" as other

type RegionCode   = String[2, 2]
type DistrictCode = String[3, 3]

type Person {
	name String required
	age  Integer
	bio  String          // personal bio
}

type Address {
	street      String required     // street address
	city        String required
	zip         String
	region_code RegionCode required
	--> IN_REGION     (one) other.Region
	--> NEAR_DISTRICT (_:many) other.District // nearby
}
