// Code generated by yammm. DO NOT EDIT.
package full

import "time"

type FipsCode string

type Tier string

const (
	TierGold   Tier = "gold"
	TierSilver Tier = "silver"
)

type CountyStatus string

const (
	CountyStatusActive CountyStatus = "active"
	CountyStatusMerged CountyStatus = "merged"
)

type Auditable struct {
	CreatedAt time.Time  `json:"created_at"`
	UpdatedAt *time.Time `json:"updated_at,omitempty"`
}

type Detail struct {
	Note *string `json:"note,omitempty"`
}

type State struct {
	Code string `json:"code"`
	Name string `json:"name"`
}

type County struct {
	Fips       FipsCode                    `json:"fips"`
	Tier       Tier                        `json:"tier"`
	Status     CountyStatus                `json:"status"`
	Population int64                       `json:"population"`
	Centroid   []float64                   `json:"centroid,omitempty"`
	Aliases    []FipsCode                  `json:"aliases,omitempty"`
	CreatedAt  time.Time                   `json:"created_at"`
	UpdatedAt  *time.Time                  `json:"updated_at,omitempty"`
	HasDetail  []*Detail                   `json:"has_detail,omitempty"`
	InState    *EDGE_County_in_state_State `json:"in_state"`
}

type EDGE_County_in_state_State struct {
	Since int64 `json:"since"`
	Where struct {
		Code string `json:"code"`
	} `json:"where"`
}

type Graph struct {
	State  []*State  `json:"State,omitempty"`
	County []*County `json:"County,omitempty"`
}

// serializedSources holds every source in the import closure, keyed by
// module-root-relative path, as verbatim .yammm text. Read it through
// SerializedSources below.
var serializedSources = map[string]string{
	"full.yammm": "schema \"full\"\n\ntype FipsCode = String [5, 5]\ntype Tier = Enum [\"gold\", \"silver\"]\n\nabstract type Auditable {\n\tcreated_at Timestamp required\n\tupdated_at Timestamp\n}\n\npart type Detail {\n\tnote String\n}\n\ntype State {\n\tcode String primary\n\tname String required\n}\n\ntype County extends Auditable {\n\tfips FipsCode primary\n\ttier Tier required\n\tstatus Enum [\"active\", \"merged\"] required\n\tpopulation Integer required\n\tcentroid Vector [2]\n\taliases List <FipsCode>\n\t--> IN_STATE (one) State {\n\t\tsince Integer required\n\t}\n\t*-> HAS_DETAIL (many) Detail\n}\n",
}

// SerializedEntry is the entry-point key into SerializedSources.
const SerializedEntry = "full.yammm"

// SerializedSources returns every source in the import closure, keyed by
// module-root-relative path. Re-load with:
//
//	schema.LoadSourcesWithEntry(ctx, SerializedSources(), SerializedEntry, "",
//		schema.WithSourcesOnly(), schema.WithSyntheticRoot("embedded://your-app"))
//
// The synthetic root is what keeps the loaded type identities stable. Passing
// module root "." instead also re-loads, but "." canonicalizes against the
// process working directory, which then lands inside every TypeID.
func SerializedSources() map[string][]byte {
	m := make(map[string][]byte, len(serializedSources))
	for k, v := range serializedSources {
		m[k] = []byte(v)
	}
	return m
}

const SchemaHash = "sha256:02066b2758597693190743205cdd0d2534b27bd31d8413d50de1a9b3d41abf04"
