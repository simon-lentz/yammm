// Code generated by yammm. DO NOT EDIT.
package geo

type RegionCode string

type City struct {
	Name      string     `json:"name"`
	Region    RegionCode `json:"region"`
	HasMarker []*Marker  `json:"has_marker,omitempty"`
}

type Marker struct {
	ID string `json:"id"`
}

type Located struct {
	Region    RegionCode `json:"region"`
	HasMarker []*Marker  `json:"has_marker,omitempty"`
}

type Graph struct {
	City []*City `json:"city,omitempty"`
}

// SerializedModel maps every source in the import closure (by module-root-relative
// path) to its verbatim .yammm content; SerializedModelEntry is the entry-point key.
// Re-load with:
//
//	schema.LoadSourcesWithEntry(ctx, toBytes(SerializedModel), SerializedModelEntry, ".")
//
// where toBytes converts each map value to []byte. Add schema.WithSourcesOnly()
// to guarantee the re-load never reads the filesystem. Module root "."
// canonicalizes against the process working directory, so that directory lands
// inside every TypeID the re-loaded schema carries. Prefer
// SerializedSources/SerializedEntry below, which is one shape for every
// generated package and takes a synthetic root instead.
var SerializedModel = map[string]string{
	"inherit_common.yammm": "schema \"common\"\n\ntype RegionCode = String [5, 5]\n\npart type Marker {\n\tid UUID primary\n}\n\nabstract type Located {\n\tregion RegionCode required\n\t*-> HAS_MARKER (many) Marker\n}\n",
	"inherit_main.yammm":   "schema \"geo\"\n\nimport \"inherit_common.yammm\" as common\n\ntype City extends common.Located {\n\tname String primary\n}\n",
}

const SerializedModelEntry = "inherit_main.yammm"

// SerializedEntry is the entry-point key into SerializedSources.
const SerializedEntry = SerializedModelEntry

// SerializedSources returns every source in the import closure, keyed by
// module-root-relative path. Re-load with:
//
//	schema.LoadSourcesWithEntry(ctx, SerializedSources(), SerializedEntry, "",
//		schema.WithSourcesOnly(), schema.WithSyntheticRoot("embedded://your-app"))
//
// The synthetic root is what keeps the loaded type identities stable. Passing
// module root "." instead also re-loads, but "." canonicalizes against the
// process working directory, which then lands inside every TypeID.
func SerializedSources() map[string][]byte {
	m := make(map[string][]byte, len(SerializedModel))
	for k, v := range SerializedModel {
		m[k] = []byte(v)
	}
	return m
}

const SchemaHash = "sha256:86af9a127a349ec8d9c68910ce6c58377541855a94cf41de9c3f02c24e938520"
