schema "test"

// Inferred from Neo4j database.
// TODO: Replace String types with UUID, Pattern, or Enum where appropriate.
// TODO: Review inferred relationship field names.
// TODO: Add invariants if needed.

type Company {
    id String primary // TODO: verify type (could be UUID, Pattern, or Enum)
}

type Employee {
    id String primary // TODO: verify type (could be UUID, Pattern, or Enum)
    --> WORKS_AT Company
}
