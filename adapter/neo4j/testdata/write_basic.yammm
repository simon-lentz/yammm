schema "write_test"

type Publisher {
    publisher_id   String primary
    name        String required
    state       String
    --> PUBLISHES (_:many) Book
}

type Book {
    publisher_id String primary
    book_id  String primary
    title     String required
    amount    Float
    tags      List<String>
    --> BY_PUBLISHER (_:one) Publisher
}
