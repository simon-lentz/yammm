schema "underscore_nil"

// Schema for testing _ (underscore) as nil literal in invariants.
type Record {
	id          String primary
	name        String required
	description String

	// Invariant using _ for nil check:
	// description is nil OR description is not empty string
	! "description_when_present"
		description == _ || description != ""
}
