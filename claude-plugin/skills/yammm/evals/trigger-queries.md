# Trigger Evaluation Queries -- yammm

## Should Trigger (10)

1. "What types does yammm support for primary keys?"
2. "Write Go code to load a .yammm schema and validate instance data"
3. "How do I export a yammm graph to Neo4j?"
4. "Explain the difference between associations and compositions in yammm"
5. "What does error code E_SCHEMA_MISSING_PRIMARY mean?"
6. "How do I write an invariant that checks all items in a collection?"
7. "Show me how to use yammm validate and yammm fmt from the CLI"
8. "I need to import types from another .yammm schema file"
9. "How does constraint narrowing work in yammm type inheritance?"
10. "Walk me through the yammm pipeline: schema load, instance validate, graph build, export"

## Should NOT Trigger (10)

1. "Write a Go HTTP handler that validates JSON input"
2. "Review this Go code for outdated patterns"
3. "Set up a Neo4j database on Kubernetes"
4. "Write a Cypher query to find shortest paths"
5. "Explain how ANTLR4 grammars work"
6. "Write a JSON Schema for my API response types"
7. "How do I write a GraphQL schema?"
8. "Set up golangci-lint for my Go project"
9. "Compare yammm to Protobuf" (opinion/comparison, not usage)
10. "Write a Python script to parse CSV data"
