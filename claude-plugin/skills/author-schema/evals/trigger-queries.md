# Trigger Evaluation Queries -- author-schema

## Should Trigger (10)

1. "Design a yammm schema for a library catalog system"
2. "Create a .yammm file that models user accounts with roles and permissions"
3. "I have a CSV with columns name, email, department, manager -- model this in yammm"
4. "Scaffold a schema for an e-commerce product inventory"
5. "Write a yammm schema from these requirements: patients, doctors, appointments"
6. "Model a social network with users, posts, comments, and follows in yammm"
7. "I need to design a data model in yammm for IoT sensor readings"
8. "Help me create types and relationships for a project management domain"
9. "Write a .yammm schema that captures financial transactions with audit trails"
10. "Generate a yammm schema for a content management system with articles, authors, and tags"

## Should NOT Trigger (10)

1. "What does error E_INSTANCE_MISSING_FIELD mean?" (diagnostics, not authoring)
2. "Review this existing schema for quality issues" (review-schema territory)
3. "How do I run yammm validate from the CLI?" (CLI usage, not schema design)
4. "Write Go code to load and validate against my schema" (API usage)
5. "Explain how invariant expressions work in yammm" (knowledge question)
6. "Export my graph to Neo4j Cypher" (adapter usage)
7. "Install yammm on my machine" (setup territory)
8. "Fix the syntax error on line 15 of my schema" (debugging, not design)
9. "What types can be primary keys?" (type system question)
10. "Write a JSON Schema for my REST API" (not yammm)
