{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "command -v yammm-lsp >/dev/null 2>&1 || echo 'Warning: yammm-lsp binary not found in PATH. LSP features (diagnostics, completion, hover) will not work. Download a pre-built binary from https://github.com/simon-lentz/yammm/releases/latest (recommended), or build from source: git clone https://github.com/simon-lentz/yammm.git && cd yammm && make build. Run /setup-lsp for guided installation.'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
