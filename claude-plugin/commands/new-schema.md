---
name: new-schema
description: Scaffold a new .yammm schema file with boilerplate type definitions
argument-hint: "<schema-name>"
---

# new-schema

Use the **schema-author** agent to scaffold a new `.yammm` schema file for the given schema name. Pass through any details the user provided (entity names, domain context, output path).
