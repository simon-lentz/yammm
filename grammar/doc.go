// Package grammar contains the ANTLR4-generated lexer, parser, listener, and
// visitor for the YAMMM DSL.
//
// This package exposes generated ANTLR artifacts (token types, parser contexts,
// listener/visitor interfaces) for use by tooling that needs low-level access
// to the token stream or parse tree — such as formatters and language servers.
//
// For schema loading and validation, prefer the higher-level entry points in
// [github.com/simon-lentz/yammm/schema/load].
package grammar
