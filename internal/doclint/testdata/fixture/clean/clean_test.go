package clean_test

// TestWidget_Builds sits in an external test package, so resolving the anchor
// in Build's doc proves the directory rather than the package is the unit.
func TestWidget_Builds() {}
